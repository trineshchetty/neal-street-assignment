# {{ ansible_managed }}
# ------------------------------------------------------------------------------
# Application Environment Variables
# ------------------------------------------------------------------------------
# Read by the Gunicorn systemd unit via EnvironmentFile directive.
# Permissions: 0600, owned by the app user — not world-readable.
# ------------------------------------------------------------------------------

COMMIT_SHA={{ commit_sha }}
SECRET_NAME={{ secret_name }}
AWS_DEFAULT_REGION={{ aws_region }}
GUNICORN_LOG_LEVEL=info
